# Ductus Sample2 (Chat Demo)

Internal app with agentic workflows, skills, and reactions. **Note:** This app uses an older ductus API and needs to be updated (e.g. `DuctusMultiplexer` → `BlockingMultiplexer` + sequencer, `CreateKernelOptions` requires `sequencer`). Track progress in RFC tasks.
